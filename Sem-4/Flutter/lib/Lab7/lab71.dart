import 'package:flutter/material.dart';
class Demo7 extends StatelessWidget {
  const Demo7({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("Hello World", style: TextStyle(fontSize: 100,),)
        ],
      ),
    );
  }
}
